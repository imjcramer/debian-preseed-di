# Opt-in NVIDIA graphics baseline. This fragment is applied only when
# addon/nvidia is selected and an NVIDIA PCI display adapter is detected.
# Hybrid systems still use auto-detected integrated GPU classes.
d-i pkgsel/include string firmware-misc-nonfree nvidia-driver-595-open nvidia-settings nvidia-suspend-common nvidia-vaapi-driver nvidia-vdpau-driver libnvidia-egl-wayland1 switcheroo-control libelf-dev clang lld llvm
d-i pkgsel/include seen true
