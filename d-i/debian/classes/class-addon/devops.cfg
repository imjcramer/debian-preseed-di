# Development and CI tooling baseline. Package names are kept in the class
# fragment so this addon remains package-selection only until its late helper
# stages the /pool-backed build/cache policy.
d-i pkgsel/include string just rustup make gh/trixie glab build-essential pkg-config cmake ninja-build black flake8 curl wget python3-venv python3-pip pipx clang pre-commit clang-format clang-tidy cppcheck mypy bandit shellcheck yamllint yq jq jsonlint jsonschema-jv markdownlint gitlint sqlfluff ansible-lint ripgrep fd-find bat direnv hyperfine sccache
d-i pkgsel/include seen true
