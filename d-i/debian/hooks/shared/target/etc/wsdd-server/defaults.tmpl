# Managed by debian-preseed-di.
#
# Bind wsdd to the interfaces selected during install so desktop file managers
# do not need to probe every interface when discovering SMB shares.

WSDD_PARAMS="__INSTALLER_WSDD_PARAMS__"
