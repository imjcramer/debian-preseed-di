# Managed by debian-preseed-di.
#
# Wi-Fi stays enabled and NetworkManager-managed for desktop users even when the
# preseed Wi-Fi addon is not selected. Do not randomize the hardware address
# during background scans; selected addon/wifi installs are the only path that
# generates a preseeded Wi-Fi connection.

[device]
wifi.scan-rand-mac-address=no
